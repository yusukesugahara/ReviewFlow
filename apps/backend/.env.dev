NODE_ENV=development

PORT=3000

# Next.js サーバー → 本 API（共有秘密。NEXT_PUBLIC_ にしない）
INTERNAL_API_KEY=dev-internal-key-change-me

# Nest が署名・検証に使う秘密鍵（十分に長いランダム文字列）
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

DB_PATH=var/sqlite.db

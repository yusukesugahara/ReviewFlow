# ReviewFlow

ReviewFlow は、自治体における補助金申請、施設利用申請、各種届出などを想定した、申請・承認ワークフロー管理アプリです。

申請フォームの作成、外部ユーザーからの申請受付、承認、差し戻し、再提出、CSV 出力、監査ログ確認までを、一連の業務フローとして管理できます。

一般的なフォーム送信だけでは扱いにくい、提出後の確認、不備の指摘、修正依頼、再提出、承認履歴の確認までを同じ流れで扱えるようにしています。

設計では、利用者が迷わず操作できること、運用後に状況を追跡しやすいこと、変更に強い構成にすることを重視しました。特に意識した点は以下です。

- ログインアカウントを持たない外部ユーザーでも申請できること
- 申請後に、誰が確認し、どこで止まり、何を直すべきか分かること
- 差し戻し内容を項目単位で明確にし、再提出まで追跡できること
- 権限、状態遷移、監査ログを整理し、実務に近い業務フローとして扱えること
- フロントエンドとバックエンドの責務を分け、保守しやすい構成にすること

業務要件、API 仕様、状態遷移などの設計詳細は `docs/` に整理しています。

---

## デモ環境

以下の URL からデモ環境を確認できます。

https://review-flow-frontend.vercel.app/login

### デモ用アカウント

```txt
Email: admin@reviewflow.demo
Password: Password123!
```

※ 無料サービスを利用しているため、初回アクセス時にバックエンドの起動まで時間がかかる場合があります。  
「API サーバーに接続できません。バックエンドの起動と環境変数を確認してください。」と表示された場合は、少し時間を置いてから再度ログインしてください。

※ デモ環境のため、自由に操作していただいて問題ありません。
デモデータはデプロイごとに初期状態へ戻ります。

---

## プロジェクト概要

| 項目 | 内容 |
|---|---|
| 開発種別 | 新規開発 |
| アプリ種別 | 外部ユーザーからの申請受付に対応した申請・承認ワークフロー管理アプリ |
| 想定領域 | 自治体・行政手続き、公共施設などの申請承認業務 |
| 想定利用シーン | 補助金申請、施設利用申請、各種届出、変更届、許可申請など |
| 担当範囲 | 課題設定、要件整理、情報設計、UI 設計、フロントエンド実装、バックエンド実装、DB 設計、テスト、ドキュメント作成 |
| 主な目的 | 申請、承認、差し戻し、再提出、履歴確認が分散しがちな業務を、ひとつの業務フローとして管理できるようにすること |
| 重視したこと | 顧客課題の理解、外部申請者の利用ハードル低減、業務状態の可視化、権限と責任範囲の明確化、差し戻し内容の具体化、監査ログによる追跡性、保守しやすい設計 |

### 技術構成と設計の要点

| 技術観点 | 実装内容 | 主な参照先 |
|---|---|---|
| Next.js / TypeScript | App Router、Server Component / Server Action、strict TypeScript による画面と業務 UI を実装 | `apps/frontend/src/app/`, `apps/frontend/src/components/` |
| Relay Server Specification | `Node` interface、global ID、Connection / PageInfo、cursor pagination を NestJS GraphQL に実装 | `apps/backend/src/app/graphql/relay-node.resolver.ts`, `apps/backend/src/common/graphql/relay-pagination.ts` |
| GraphQL Fragment / Relay compiler | 申請詳細・一覧の取得項目を Fragment 化し、`relay-compiler` で schema / operation / generated artifact を検証 | `apps/frontend/src/lib/relay/application-operations.graphql.ts`, `apps/frontend/src/lib/relay/__generated__/` |
| Jest / Playwright | バックエンドの unit / integration、フロントエンドの Jest / Playwright E2E を用意 | `apps/backend/src/**/*.spec.ts`, `apps/frontend/__test__/`, `apps/frontend/e2e/` |
| CI / 品質ゲート | GitHub Actions で lint、Relay validate、型チェック、テスト、ビルドを実行 | `.github/workflows/ci.yml`, `package.json` |
| 権限 / 状態遷移 | 申請の閲覧・承認可否と、提出・承認・差し戻し・再提出の状態遷移を policy に集約 | `ApplicationAccessPolicy`, `ApplicationTransitionPolicy` |
| デザインシステム | Button / Input / Badge / 申請ステータス / 承認進捗を Storybook で確認できるように整理 | `apps/frontend/src/stories/` |

Relay 周りは、フロントエンド側で Fragment / Query を compiler 管理し、バックエンド側で Node / Connection を実装する構成にしています。申請一覧は Relay Connection 形式で返し、cursor を DB の `skip/take` に変換してページ単位で取得します。

---

## アプリ概要

ReviewFlow では、管理者、申請者、承認者の 3 つの立場を想定しています。

管理者は申請フォームと承認フローを作成し、申請者は公開フォームから申請を提出します。承認者は申請内容を確認し、必要に応じて承認、却下、差し戻しを行います。

差し戻し時には、修正対象の項目とコメントを申請データに紐づけ、申請者が内容を修正して再提出できるようにしています。最終承認後は、CSV 出力や監査ログで履歴を確認できます。

主な流れは以下です。

1. 管理者が申請フォームを作成する
2. 外部ユーザーまたは社内ユーザーが申請を提出する
3. 承認者が申請内容を確認する
4. 必要に応じて承認・却下・差し戻しを行う
5. 申請者が差し戻し内容を確認し、修正して再提出する
6. 最終承認後、CSV 出力や監査ログで履歴を確認する

---

## 開発背景・課題設定

申請業務をデジタル化する手段としては、Google フォーム、Excel、メール、紙、専用ワークフローシステムなどがあります。

ただ、実際の業務では、フォームで受け付けるだけでは完結しないケースがあります。  
たとえば、記入漏れ、添付書類の不足、金額の確認、申請内容の修正などにより、承認者が申請者に差し戻しを行い、申請者が修正して再提出する必要があります。

このとき、申請と承認、差し戻し連絡、再提出、履歴確認が別々のツールに分かれていると、以下のような課題が発生します。

- どの申請が現在どの状態なのか分かりにくい
- 誰が次に対応すべきか分かりにくい
- 差し戻し時に、どの項目を修正すべきか伝わりにくい
- 再提出後に、何が修正されたのか追いにくい
- 承認履歴や操作履歴を後から確認しづらい
- CSV 出力や監査ログ確認が属人化しやすい

この課題に対して、ReviewFlow では申請フォーム、承認フロー、差し戻し内容、再提出履歴、申請状態、CSV 出力、監査ログを同じ業務フロー上で扱う構成にしています。

---

## UI・デザイン方針

ReviewFlow の UI では、見た目の装飾性よりも、業務で迷わず使えることを重視しました。

申請業務では、管理者、承認者、申請者で必要な情報や操作が異なります。  
そのため、画面ごとの役割を明確にし、ユーザーが「今、何を確認すべきか」「次に何をすればよいか」を判断しやすい構成を目指しました。

### 操作目的を明確にする

申請者には、申請入力、修正対象の確認、再提出を中心に表示します。  
承認者には、申請内容、承認状況、差し戻し履歴、承認・差し戻しボタンを中心に配置します。  
管理者には、申請フォーム、承認フロー、提出済み申請、監査ログなど、管理対象ごとに画面を分けて表示します。

ユーザーの役割ごとに必要な操作を整理することで、画面を見たときに「自分は何をすればよいか」が分かることを意識しました。

### 状態を見ただけで分かるようにする

申請中、承認済み、差し戻し、却下などの状態をラベルで表示し、一覧画面や詳細画面で現在の状態を把握しやすくしています。

申請業務では、状態が分からないことが確認漏れや催促の属人化につながります。  
そのため、現在の対応者や承認ステップが分かることを重視しました。

### 差し戻し内容を具体的に伝える

差し戻し時には、対象項目とコメントを申請データに紐づけています。

これにより、申請者は「どの項目を、なぜ修正すべきか」を確認しながら再提出できます。  
承認者側も、過去の修正依頼や再提出内容を確認しながら審査を進められます。

差し戻しを単なるコメントではなく、業務フロー上の重要な状態として扱った点が、このアプリの特徴です。

### 外部ユーザーでも申請しやすくする

申請者は、必ずしも業務システムに慣れているとは限りません。

そのため、外部申請者向けにはログインアカウントを不要にし、申請に必要な情報入力に集中できる構成にしました。

住民や事業者など、組織外のユーザーでも迷わず申請できるようにし、申請者側の利用ハードルを下げる構成にしています。

---

## 主な機能

ReviewFlow では、管理者、申請者、承認者の 3 つの利用者を想定して機能を整理しています。

### 管理者向け

#### 申請フォーム作成

スペース単位で申請フォームを作成できます。  
申請内容に応じてフィールドを追加・並べ替えでき、作成したフォームは公開・アーカイブできます。

[![Image from Gyazo](https://i.gyazo.com/b2db14399067f9d8a43124bd48b8e314.gif)](https://gyazo.com/b2db14399067f9d8a43124bd48b8e314)

#### 承認フロー設定

申請フォームに対して、承認ステップや担当者を設定できます。  
複数人での確認や、段階的な承認が必要な申請業務を想定しています。

#### 申請一覧確認

スペース内の提出済み申請を一覧で確認できます。  
ステータスや日付をもとに、現在どの申請が対応中なのかを把握できます。

#### CSV 出力

申請データを CSV として出力できます。  
集計や内部確認、他システムでの利用を想定し、CSV エクスポートジョブを作成して、完了後にダウンロードできる構成にしています。

#### 監査ログ確認

申請、承認、差し戻し、ユーザー招待、権限変更、申請フォーム作成・編集などの操作履歴を確認できます。  
誰が、いつ、何を行ったかを確認できるようにし、問い合わせ対応や内部確認に使える設計にしています。

---

### 申請者向け

#### ログイン不要での申請提出

申請者は、ログインアカウントを持っていなくても申請を提出できます。  
外部ユーザーからの申請受付を想定し、アカウント作成の手間を省ける点を特徴としています。

[![Image from Gyazo](https://i.gyazo.com/321915af61fd966902a9468eea0534f7.gif)](https://gyazo.com/321915af61fd966902a9468eea0534f7)

#### 申請フォーム入力

申請フォームに設定された項目に沿って、申請内容を入力できます。  
住民・事業者・取引先などの外部ユーザーでも使いやすいよう、必要な項目を順に入力できる構成にしています。

[![Image from Gyazo](https://i.gyazo.com/e91a03ad3a53c2eb5dbb7365af78907e.gif)](https://gyazo.com/e91a03ad3a53c2eb5dbb7365af78907e)

#### 差し戻し内容の確認

申請が差し戻された場合、差し戻し対象の項目、項目別コメント、全体コメント、差し戻し履歴を確認できます。

#### 修正後の再提出

差し戻し後、申請者は修正内容を反映して再提出できます。  
不備のある申請を修正し、承認に進めるための流れを扱えるようにしています。

---

### 承認者向け

#### 申請内容確認

申請者が提出した内容、承認進捗、過去の差し戻し履歴を確認できます。

#### 承認

現在の承認ステップに割り当てられた担当者が、申請内容を確認して承認できます。  
承認フローに沿って、次のステップまたは完了状態へ進めます。

#### 却下

承認できない申請について、承認フローを終了させる操作として却下できます。

#### 差し戻し

申請内容に不備がある場合、フィールド単位で差し戻し対象を選び、コメント付きで申請者に修正依頼できます。

---

## 設計上の工夫

### 状態遷移を持つ業務ワークフロー

ReviewFlow は、単純な CRUD アプリではなく、申請の状態に応じて操作可能な内容が変わる業務ワークフローとして設計しています。

申請は、下書き、審査中、差し戻し、承認済み、却下といった状態を持ちます。  
提出、承認、差し戻し、再提出、却下の操作に応じて状態が遷移します。

特に重視したのは、フォーム送信後の「差し戻し」と「再提出」です。  
申請内容に不備があった場合、承認者が修正対象を指定して差し戻し、申請者が内容を修正して再提出できるようにしています。

状態遷移のルールは `ApplicationTransitionPolicy` に集約し、サービス層から直接ステータスを書き換えないようにしました。  
これにより、状態変更のルールが分散しない構成にしています。

---

### 実務を想定した権限管理

ReviewFlow では、単純な管理者・一般ユーザーというロールだけでなく、申請の状態や承認フロー上の担当者設定に応じて操作権限を判定しています。

たとえば、承認操作は、審査中の申請に対して、現在の承認ステップに割り当てられた担当者、またはテナント管理者のみが実行できるようにしています。

`ApplicationAccessPolicy` では、以下の条件を組み合わせて閲覧・操作権限を判定しています。

- テナント管理者かどうか
- 申請者本人かどうか
- 現在の承認ステップの担当者かどうか
- 過去に承認操作へ関与したユーザーかどうか
- 対象スペースにアクセスできるユーザーかどうか

フロントエンドでも操作ボタンの表示制御を行いますが、最終的な権限判定はバックエンド側で行う構成にしています。  
これにより、画面上の表示制御だけに依存せず、API レベルでも不正な操作を防げるようにしています。

---

### 監査ログによる操作履歴の管理

業務システムでは、処理が完了するだけでなく、後から「誰が・いつ・何をしたか」を確認できることが重要です。

ReviewFlow では、申請、承認、差し戻し、却下、ユーザー招待、権限変更、申請フォーム作成・編集などの操作を監査ログとして記録しています。

これにより、問い合わせ対応や内部確認が必要になった場合でも、操作履歴をもとに状況を追跡できます。

---

### Relay compiler / Relay runtime による API 連携

ReviewFlow では、フロントエンドとバックエンドの内部連携に Relay compiler と Relay runtime を利用しています。

バックエンドでは NestJS GraphQL resolver により業務操作の query / mutation を公開しています。  
フロントエンドでは `relay-compiler` で GraphQL schema と operation / fragment の整合性を検証し、生成された artifact の operation text を `relay-runtime` の server-side Environment / Network で実行しています。

申請詳細などの複数画面で使う取得項目は Fragment として分け、`ApplicationSummaryFields`、`ApplicationDetailFields`、`ApplicationProgressStepFields` などを operation から参照しています。
これにより、取得項目の変更時に GraphQL schema との不整合を compiler で検出できるようにしています。

REST パスをフロントエンドのクライアント API として扱わず、`client.login(...)` や `client.createApplication(...)` のような業務 operation 名で呼び出すことで、画面側の意図を明確にしています。

---

### バックエンドの責務分離

バックエンドでは、Controller や Service に業務ルールを詰め込みすぎないよう、責務ごとにクラスやサービスを分けています。

| クラス / モジュール | 役割 |
|---|---|
| ApplicationAccessPolicy | 申請の閲覧・レビュー操作が許可されるかを判定 |
| ApplicationTransitionPolicy | 申請状態の前提チェックと状態遷移 |
| ApplicationFormValueValidator | フォーム入力値が定義どおりかを検証 |
| ApplicationSubmissionService | 提出・再提出処理の流れを管理 |
| ApplicationReviewActionService | 承認・却下・差し戻し処理の流れを管理 |
| ApplicationCorrectionService | 差し戻し対象や修正依頼に関する処理 |
| Repository 層 | 永続化処理を担当 |

この分離により、仕様変更が発生した場合に、権限、状態遷移、入力検証、永続化のどこを修正すべきかを判断しやすくしています。

たとえば、差し戻し条件を変更する場合は差し戻し関連のサービス、ステータス遷移のルールを変更する場合は `ApplicationTransitionPolicy` を中心に確認できるため、Service が肥大化しにくい構成にしています。

---

## 開発で意識したこと

ReviewFlow では、申請業務で起こりやすい手戻りや確認漏れを減らせるように、業務フロー、権限、状態管理、保守性を意識して開発しました。

特に以下の点を重視しています。

- 申請後の確認、差し戻し、修正、再提出、履歴確認までを一連の業務として扱うこと
- ログイン不要の外部申請者を想定し、申請の利用ハードルを下げること
- 申請状態、承認ステップ、担当者に応じて操作権限を判定すること
- 主要操作を監査ログに残し、後から追跡できるようにすること
- 状態遷移や認可ルールを単体で検証しやすい構成にすること
- Relay / GraphQL operation と共有 DTO 型により、フロントエンドとバックエンドの API 契約を明確にすること
- UI の分かりやすさと、変更に強いバックエンド設計を両立すること

---

## 今後の改善予定

今後は、機能追加だけでなく、実際の利用状況を見ながら申請体験、承認体験、管理体験を改善していく想定です。

### 機能面

- ファイル添付への対応
- 金額や項目値に応じた承認フロー分岐
- 既存承認フローの再利用 UI
- メール通知テンプレートの拡充
- 本番運用向けの SMTP 設定・送信失敗時の運用設計

### 品質・テスト

- 申請、承認、差し戻し、再提出までの E2E テスト拡充
- 権限管理ロジックのさらなる共通化
- フロントエンドの主要操作に対するテスト追加

### 運用・プロダクト

- 本番運用を想定したデプロイ構成の整備
- 一覧画面の操作性改善
- モバイル表示の改善
- 監査ログの検索性向上
- CSV 出力や監査ログ分析機能の拡張

---

## 開発姿勢

業務システムの開発では、画面の見た目だけでなく、利用者が実際に業務を進められることを大切にしています。

ReviewFlow でも、申請フォームを作る機能そのものより、申請後に誰が確認し、どこで止まり、何を直し、後から何を確認できるのかを明確にすることを重視しました。

今後も、顧客や利用者の課題を理解し、業務で使いやすく、運用しやすく、変更にも強いシステムを設計・実装することを重視していきます。

# ReviewFlow

## 概要
ReviewFlow は、組織ごとに独立したワークスペースを持ち、各組織が自分たち専用の申請フォーム・承認フロー・申請データ・監査ログを管理できる、マルチテナント対応の申請ワークフローSaaSである。

本プロダクトは、差し戻しや再提出が多い申請業務を対象に、組織内ユーザーだけでなく **組織外の申請者** も想定する。テナントのアカウント登録をせず、メールで届く申請者用リンク（アクセストークン）から申請・差し戻し修正・再提出まで行えることを価値とする。

本プロダクトは、上記を含む申請業務を対象に、以下を一元管理する。

- 申請フォーム定義
- 承認フロー定義
- 申請作成 / 提出 / 再提出
- 修正依頼付き差し戻し
- 承認 / 却下
- CSV出力
- 監査ログ
- テナントごとのユーザー管理（**テナント管理者によるユーザー招待**を含む）
- ログイン中ユーザーのアカウント管理（プロフィール、メールアドレス変更、パスワード変更）
- **使用状況ダッシュボード**（`/space`、テナント管理者向け）

## ポートフォリオとしての評価軸

ReviewFlow は、単なる CRUD や管理画面テンプレートではなく、実務に近い申請・承認ワークフローを扱うポートフォリオとして設計する。

実装・レビュー・README 改善では、以下を重視する。

- 申請、承認、差し戻し、修正、再提出、却下までが自然な業務フローとして表現されていること
- 承認者、申請者、テナント管理者、スペース管理者の責務と操作範囲が明確であること
- フロントエンドの表示制御だけでなく、バックエンドで認可を必ず担保していること
- `tenantId` と `groupId` によるテナント・スペース分離が一覧、詳細、更新、削除、CSV 出力、監査ログで守られていること
- 不正な状態遷移を防ぎ、`draft` / `published` / `in_review` / `returned` / `approved` / `rejected` の意味が崩れないこと
- 差し戻し時に、修正対象項目とコメントを保持し、再提出まで追跡できること
- 重要操作が監査ログに残り、誰がいつ何をしたか確認できること
- 監査ログで、申請だけでなくユーザー招待、ユーザー削除・復活、スペース更新、スペースメンバー変更などのユーザー操作も追跡できること
- Next.js と NestJS の責務分離、OpenAPI による型安全な連携、テストしやすい業務ロジック分離が説明できること

AI エージェントで作業する場合は、`.agents/skills/reviewflow-portfolio/SKILL.md` を入口にし、必要に応じて同 Skill の `references/` と本 `docs/` を参照する。

## ドキュメント一覧

GitHub 上では Markdown の表と Mermaid 図がそのまま表示される。設計の全体像を確認するときは、以下の順で読む。

| 順番 | ドキュメント | 内容 | 主な読みどころ |
| --- | --- | --- | --- |
| 1 | [概要](00_overview.md) | プロダクトの目的とMVPスコープ | 解決したい課題、コア機能 |
| 2 | [業務要件](01_business_requirements.md) | 利用者、業務フロー、制約 | MVPで扱う業務範囲 |
| 3 | [ドメインモデル](02_domain_model.md) | 主要概念と責務 | テナント、スペース、申請、承認 |
| 4 | [ER図](03_er_diagram.md) | テーブル定義と関係 | Mermaid ER図、インデックス方針 |
| 5 | [API仕様](04_api_spec.md) | API契約と代表エンドポイント | OpenAPI連携、レスポンス方針 |
| 6 | [全体構成](05_architecture.md) | フロントエンド、バックエンド、DBの責務 | Mermaid構成図、非同期処理 |
| 7 | [バックエンド設計](06_backend_design.md) | NestJS側の責務分離 | Module、Service、DTO、認可 |
| 8 | [フロントエンド設計](07_frontend_design.md) | Next.js側の責務分離 | Server Components、BFF、UI構成 |
| 9 | [認証とマルチテナント](08_auth_and_multitenant.md) | 認証、認可、テナント分離 | ロール、スペース権限 |
| 10 | [申請・承認ワークフロー](09_workflow_and_approval.md) | ステータス遷移と承認ルール | Mermaid状態遷移図 |
| 11 | [差し戻し機能](10_correction_feature.md) | 修正依頼と再提出 | フィールド単位の指摘 |
| 12 | [コーディングルール](11_coding_rules.md) | 実装時の規約 | 変更時の判断基準 |
| 13 | [タスク](12_tasks.md) | 実装タスク管理 | 残タスクと進捗 |
| 14 | [Codexプロンプト例](13_codex_prompt_examples.md) | エージェント作業例 | 依頼テンプレート |
| 15 | [UIデザインルール](14_ui_design_rules.md) | UI実装時の設計ルール | shadcn/ui、Tailwind、状態表示、アクセシビリティ |
| 16 | [開発ガイド](15_development_guide.md) | 環境構築と開発手順 | Docker、テスト、OpenAPI 型生成 |

## 管理画面（テナント向け）
契約組織の **テナント管理者（tenant_admin）** が参照する画面。自組織ワークスペース内の利用状況を把握するためのものであり、プラットフォーム運営者専用のコンソールとは別とする。

実装では、申請件数・平均差し戻し数・再提出件数などの指標は **`/space` ダッシュボード** に表示する。`/admin` はスペース管理・ユーザー招待・監査ログへの入口であり、使用状況の集計画面ではない。

表示する指標の例:

- **申請件数**（期間内など、集計単位は実装時に定義）
- **平均差し戻し数**（例: 申請あたりの差し戻し回数の平均）
- **再申請（再提出）件数**（再提出に至った件数・回数など、定義は実装時に明文化）

## 解決したい課題
既存の申請業務では、以下が分断されがちである。

- フォーム入力
- 差し戻し連絡
- 修正箇所の指摘
- 再提出
- 承認進捗の可視化
- 集計 / CSV出力

特に、差し戻し時に「どの項目を直すべきか」が曖昧で、メールや電話で補完しているケースを改善する。

組織外の申請者にとっては、差し戻しのたびにアカウント発行や社内システムへのログインを求めると手続きが止まりやすい。ReviewFlow では、案内メールや差し戻し通知メールに含まれる URL から `/apply/form` や `/apply/correction` にアクセスし、**テナント未登録のまま** 修正対象項目の確認と再提出ができる。

## コア機能
- マルチテナント
- ロールベース認可
- 動的フォーム定義
- 承認フロー定義
- 修正依頼付き差し戻し
- **公開申請**（申請者アクセストークンによるログイン不要の申請・差し戻し修正・再提出）
- 非同期CSV出力
- 監査ログ
- テナント向けダッシュボード（`/space` で使用状況を可視化）

## MVPスコープ
- テナント作成（サインアップ時にテナントと初回 `tenant_admin` を同時作成）
- テナント管理者作成
- メンバー招待（**テナント管理者（tenant_admin）** が自テナントにユーザーを招待し、承認者・申請者などを追加できる）
- パスワードリセット
- ログイン中アカウントのプロフィール、メールアドレス、パスワード変更
- ロール管理
- フォーム定義 / 公開
- 承認フロー定義
- 申請作成 / 提出
- 公開申請（組織外申請者向け。案内メール → `/apply/form`、差し戻し → `/apply/correction`）
- 承認 / 差し戻し / 再提出 / 却下
- 修正対象フィールドの指定
- CSV非同期出力
- 監査ログ
- 使用状況ダッシュボード（`/space` で申請件数・平均差し戻し数・再申請数などを表示）

## 技術スタック
### Frontend
- Next.js 16 (App Router)
- React 19
- TypeScript
- Tailwind CSS v4
- Zod
- Radix UI ベースの shadcn/ui 互換コンポーネント（手動実装）
- openapi-fetch / openapi-typescript
- Jest、Testing Library
- Playwright（最小構成）

### Backend
- NestJS 11
- TypeScript
- TypeORM
- PostgreSQL
- Swagger / OpenAPI
- Passport JWT
- Pino（nestjs-pino）
- Nodemailer
- Jest（unit / integration / e2e）

### Infra / Dev
- Docker Compose
- GitHub Actions
- npm workspaces
